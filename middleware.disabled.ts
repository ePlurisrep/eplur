// Disabled middleware (kept for reference).
// Session proxy logic has been moved to server API routes; do not run this file.

// If you need to re-enable middleware behavior, restore the original
// implementation from version control and remove the `.disabled` suffix.
