# Example environment variables
# Copy this file to `.env` and fill in your keys.
# For server-side usage set DATAGOV_API_KEY. For client/public usage (not recommended) set NEXT_PUBLIC_DATAGOV_API_KEY.
DATAGOV_API_KEY=
NEXT_PUBLIC_DATAGOV_API_KEY=

# Other project env vars (examples)
# SUPABASE_URL=
# SUPABASE_KEY=